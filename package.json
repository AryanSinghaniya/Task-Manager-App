{
  "name": "task-manager-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm start",
    "postinstall": "node -e \"if (!process.env.VERCEL) require('child_process').execSync('npm install --prefix server', { stdio: 'inherit' })\"",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
